import javax.swing.*;
import java.awt.*;

public class MainWindow extends JFrame {

    public MainWindow(){
        setTitle("SoundSergeant");
        setSize(400,400);
        setLayout(new BorderLayout());
        setVisible(true);

    }

}
